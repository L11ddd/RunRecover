# RunRecover 前端环境变量示例
# 本地开发默认指向 FastAPI 后端。
VITE_API_BASE_URL=http://127.0.0.1:8000

# 运动截图辅助填写当前默认隐藏。
# 当后端配置了支持图片理解的大模型后，可改为 true 再重启前端。
VITE_ENABLE_SCREENSHOT_UPLOAD=false

# 生产构建时改成你的后端 HTTPS API 域名，例如：
# VITE_API_BASE_URL=https://api.runrecover.example.com
